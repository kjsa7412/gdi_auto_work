큰 문제는 실행 흐름의 마지막 10%가 아직 메워지지 않았다는 점입니다. /analyze는 끝나면 current.phase를 task_analyzed로 바꾸고, 사용자가 final/people_spec.md를 검토한 뒤 /build를 실행하라고 안내합니다. 그런데 /build는 시작 조건으로 current.phase == task_reviewed를 요구합니다. runtime 모델도 task_analyzed와 task_reviewed를 별도 상태로 나누고 있는데, 현재 command 집합에는 사용자 검토 완료 후 active_context를 task_reviewed로 올리는 명시적 절차나 command가 보이지 않습니다. 이 상태면 실사용 시 /analyze 다음에 /build가 막힐 가능성이 큽니다