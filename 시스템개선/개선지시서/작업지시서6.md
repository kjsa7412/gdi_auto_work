가장 큰 문제는 계약 계층끼리 아직 서로 완전히 맞물리지 않는다는 점입니다.
대표적으로 work/.active_context.yml은 workspace_mode: "single-active"를 쓰고 있는데, active_context.schema.json의 workspace_mode enum은 task / fix / idle만 허용합니다. 또 command들은 성공 상태로 task_analyzed, task_built, fix_applied 같은 phase를 사용하지만, 같은 schema의 current.phase enum은 idle / analyze / build / fix / verify만 허용합니다. 즉, 현재 샘플 상태 파일과 schema, 그리고 command가 서로 동시에 참이 되기 어렵습니다. 이건 지금 가장 먼저 고쳐야 할 정합성 문제입니다